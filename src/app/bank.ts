export interface Bank {
    id: number;
    name: string;
    website: string;
}
// All new key,value pairs need to be added here in the interface ts file, in the banks.ts file (which is the object list), and lastly in the html file.

